# Upgrade report — base-building-kit 0.16 → 0.25

Real run, 2026-09-19. Started from commit `6b60aab` (the way back preflight recorded).
Rehearsal: [rehearsal-2026-09-19.md](rehearsal-2026-09-19.md).

## The three numbers, rehearsal beside the real run

| | Rehearsal | Real run |
|---|---|---|
| Decisions asked | 3 (all standing questions) | **0** — every question the run met was on the sheet |
| Files that genuinely differed | 0 | 0 |
| Troubleshooting steps | 0 | 0 |

The sheet was answered **defaults**.

## Taken

- Every `meta-*/SKILL.md` and `meta-foundation/INTENT.md` — 17 skills, all classified *untouched*
  (no residue anywhere in this project), so all took the staged version with no question.
- All 13 hooks, including the new `mark-done.sh`. 0.25's hook commands carry a root-finding wrapper,
  so they fire correctly when the session's working directory is a subdirectory — which this session
  hit repeatedly.
- All 8 agents, to `.claude/agents/` and `.claude/skills/agents/`.
- 10 new files under `checks/`: `G3-retired.sh`, `G4-pointers.sh`, `G5-steps.sh`, `preflight.sh`,
  `rollback.sh`, `merge.sh`, `hooks-selftest.sh`, `residue.sh`, `roots.sh`, `retired-phrases.txt`.
- `walk.sh` and `walk.expected` — the walk grew from 36 states to 47.
- The line history under `meta-bootstrap/history/`.

## Ported

Nothing. No skill in this project had ever been edited, so there was no residue to reapply and no
merge to run.

## Removed

Five files the kit no longer ships: `meta-bootstrap/history/regenerate.sh`,
`meta-mechanisms/tests/walk-004.sh`, `walk-007.sh`, and `tests/fixtures/` (two scripts).

**`P-010.sh` was kept.** The stale rule retires a `P-NNN.sh` whose precedent id is not in the
project's casebook; `P-010` is in this one — it came from C-010, contract-002 left half-implemented
— so the check is this project's own, not the base kit's evidence.

## Migrated

- `MANIFEST.yaml` — `base_kit_version: 0.16 → 0.25`; `workspace: []` added. Verified: exactly one
  line gone, two new.
- `CONTRACT-LOG.yaml` — `report-001` gained `verification_state`, `audited`, `disappointment`,
  `premortem`, `red_test`, `cost`, each `legacy`. `contract-001` and `contract-002` already carried
  every field and were not touched. Verified: zero pre-existing lines lost.
- `MAP.md` — header refreshed; the M-25 row took the staged template's text and kept its `ratified`
  status. All 30 entries present, project name preserved.
- `CORRECTIONS.yaml`, `LEDGER.yaml`, `DRIFTLOG.yaml`, `CASEBOOK.yaml`, `LEARNINGLOG.yaml` — header
  block only; no field migration was due. Verified: zero lines lost in each.
- `settings.json` — all 7 kit hook groups replaced; `permissions.deny` keeps `Read(kit-sealed/**)`;
  `additionalDirectories: []` added.
- `CLAUDE.md` — **unchanged**, as the empty workspace list predicted.

## Asked

Nothing. Every question the run met had been answered on the sheet, which is the first number being
zero.

## Verified at the end

| Check | Result |
|---|---|
| `G1-size.sh` | exit 0 |
| `G2-migration.sh` | exit 0 |
| `G3-retired.sh` | exit 0, **no note** |
| `G4-pointers.sh` | exit 0 |
| `G5-steps.sh` | exit 0 |
| `hooks-selftest.sh` | every hook behaved |
| `tests/walk.sh` | all 47 states match `walk.expected` |
| Baseline | regenerated, 98 files |

`G3-retired` produced no note this run. In the rehearsal it flagged `meta-map/MAP.md:4` because the
first pass refreshed seven record headers and skipped the map's. The real run refreshed eight.

## What this upgrade did not check

- **It fixes nothing in `report-001`.** All five findings were verified still open at 0.25 before
  the run began: the seal's blocked write, the auditor's unbounded search, the walk's fixture, the
  absence of any stalled-gate signal, and the gate never reading `type`. The upgrade brings real
  install/upgrade machinery; it does not touch the adoption loop.
- **The seal was never exercised.** `kit-sealed/` is empty and no batch exists, so no step of this
  upgrade touched the mechanism F-1 describes. It will fail the same way at the next batch.
- **The new hooks were not seen as live events.** `hooks-selftest.sh` runs each hook once against a
  throwaway copy; it does not prove behaviour inside a real Claude Code session.
- **The 47-state walk was not read.** It passes against its own expected file; which 11 states are
  new since 36, and what they assert, was not examined.
- **Nothing was re-verified about this project's own code.** `contract-002` remains implemented with
  `verification_state: none`, and P1.6 remains two clauses short.

## What this did not ask

Nothing. No question arose during the run that the sheet had not already answered.
